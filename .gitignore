*.iml
.gradle
/local.properties
/.idea
.DS_Store
/captures
.externalNativeBuild

# Generated files
build/
# Release build output (APK, baseline profiles, metadata)
/app/release/

# Signing secrets (keep the .example template, ignore the real one)
keystore.properties

# Extra (custom) settings
extra-settings.gradle
